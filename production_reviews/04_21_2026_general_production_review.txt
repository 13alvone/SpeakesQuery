[
  {
    "finding_id": "H-CE-1",
    "severity": "HIGH",
    "surface": "core_engine",
    "script_tier": null,
    "dimension": "correctness",
    "title": "SQL string values unescaped - single quotes silently return empty",
    "files": ["functionality/duckdb_index_call.py"],
    "locations": [{"type": "symbol", "name": "_parse_in_clause,_parse_operand"}],
    "problem": "_parse_in_clause (lines 427/431/433) and _parse_operand (line 445) wrap string values with f\"'{t.value}'\" without escaping embedded single quotes. A query like `title=\"O'Brien\"` produces invalid SQL, is caught by the generic except at 697, and the file is skipped - returning an empty DataFrame with no user-visible error.",
    "fix_instruction": "In functionality/duckdb_index_call.py, replace each f\"'{t.value}'\" at lines 427, 431, 433 (in _parse_in_clause) and 445 (in _parse_operand) with: \"'\" + str(t.value).replace(\"'\", \"''\") + \"'\". Do NOT change the read_parquet('{fpath}') f-string - fpath is filesystem-validated. Add tests/yaml/tier1_commands/search_apostrophe.yaml with a query against a fixture column containing apostrophes, asserting non-empty results.",
    "done_when": "pytest tests/test_spql.py -k apostrophe -vv passes; grep \"replace.*''\" functionality/duckdb_index_call.py returns 4 lines.",
    "runtime_validated": false,
    "validation_evidence": null,
    "auto_execute": false,
    "blocked_by": [],
    "estimated_edit_scope": "trivial"
  },
  {
    "finding_id": "H-CE-2",
    "severity": "HIGH",
    "surface": "core_engine",
    "script_tier": null,
    "dimension": "concurrency",
    "title": "QueryEngine AsyncIOScheduler lacks job_defaults - misfires drop silently",
    "files": ["query_engine/QueryEngine.py"],
    "locations": [{"type": "lines", "start": 44, "end": 44}],
    "problem": "scheduler = AsyncIOScheduler() is instantiated without job_defaults. A paused server, overloaded event loop, or restart mid-cron-minute drops the missed fire with only a DEBUG log. Companion ScheduledInputEngine (engine.py:112-122) correctly passes misfire_grace_time=300, coalesce=True, max_instances=1.",
    "fix_instruction": "Change query_engine/QueryEngine.py line 44 to: scheduler = AsyncIOScheduler(job_defaults={\"coalesce\": True, \"max_instances\": 1, \"misfire_grace_time\": 300}). Add tests/test_scheduler_wiring.py asserting the defaults are present on import.",
    "done_when": "grep -n 'misfire_grace_time' query_engine/QueryEngine.py returns the changed line; the new wiring test passes.",
    "runtime_validated": false,
    "validation_evidence": null,
    "auto_execute": false,
    "blocked_by": [],
    "estimated_edit_scope": "trivial"
  },
  {
    "finding_id": "H-CE-3",
    "severity": "HIGH",
    "surface": "core_engine",
    "script_tier": null,
    "dimension": "error_handling",
    "title": "process_query swallows exceptions into (None, None) - caller cannot tell empty from crashed",
    "files": ["query_engine/CmdExecutionBackend.py"],
    "locations": [{"type": "lines", "start": 115, "end": 117}],
    "problem": "run_query_and_return_results_df returns (None, None) for both 'zero rows' and 'query crashed'. A diagnostic sibling (process_query_with_diagnostics, line 175) already exists and returns a reason. UI/API callers mixed between the two hide error causes.",
    "fix_instruction": "Audit callers of process_query via `grep -rn 'process_query(' desktop_app/ query_engine/ alert_groups/`. Migrate every user-facing caller (desktop_app/server.py /api/query* routes, alert_group dispatcher feeder loop if still using the non-diagnostic variant) to process_query_with_diagnostics. At the bare-except in CmdExecutionBackend.py:115-117, raise the log level to ERROR and include exception class name: logging.error('[x] %s while processing query: %s', type(e).__name__, e).",
    "done_when": "Every /api/query* route in desktop_app/server.py imports process_query_with_diagnostics; pytest tests/test_api.py -vv still passes.",
    "runtime_validated": false,
    "validation_evidence": null,
    "auto_execute": false,
    "blocked_by": [],
    "estimated_edit_scope": "small"
  },
  {
    "finding_id": "H-CE-4",
    "severity": "HIGH",
    "surface": "core_engine",
    "script_tier": null,
    "dimension": "correctness",
    "title": "execution_history.saved_search_filename PRIMARY KEY collides across subfolders",
    "files": ["query_engine/QueryEngine.py"],
    "locations": [{"type": "lines", "start": 391, "end": 410}],
    "problem": "execution_history uses saved_search_filename TEXT PRIMARY KEY. Two saved searches with the same filename in different folders overwrite each other's telemetry. save_execution_telemetry inserts only the filename (saved_search_path.name), not the full relative path.",
    "fix_instruction": "Change the PRIMARY KEY to a composite: (saved_search_filename, execution_start_time). Add a one-shot migration in QueryEngine startup: if the legacy schema is detected, copy rows into a new table with the composite PK. Update save_execution_telemetry to insert the full relative path (saved_search_path relative to project root) in a new column saved_search_path; keep filename for legacy display. Add tests/test_execution_history_collision.py creating two searches named 'daily.yaml' in different folders and asserting both have distinct telemetry rows.",
    "done_when": "New collision test passes; legacy migration runs idempotently; pytest tests/test_api.py -vv passes.",
    "runtime_validated": false,
    "validation_evidence": null,
    "auto_execute": false,
    "blocked_by": [],
    "estimated_edit_scope": "medium"
  },
  {
    "finding_id": "H-CE-5",
    "severity": "HIGH",
    "surface": "core_engine",
    "script_tier": null,
    "dimension": "correctness",
    "title": "parquet_writer uses os.rename - not atomic on Windows",
    "files": ["scheduled_input_engine/parquet_writer.py"],
    "locations": [{"type": "lines", "start": 85, "end": 85}, {"type": "lines", "start": 211, "end": 211}],
    "problem": "os.rename fails on Windows when the target file exists. The desktop app ships for Windows per README. On overwrite=True the call can raise, leaving a stranded .tmp file and no final output. POSIX semantics mask the bug in CI.",
    "fix_instruction": "In scheduled_input_engine/parquet_writer.py, replace every 'os.rename(' with 'os.replace('. Semantics are identical on POSIX and correct (atomic overwrite) on Windows. Mirror the EBUSY fallback pattern from functionality/atomic_write.py if not already present. Add a test that writes twice to the same path with overwrite=True.",
    "done_when": "grep -n 'os.rename' scheduled_input_engine/parquet_writer.py returns zero matches; the double-write test passes.",
    "runtime_validated": false,
    "validation_evidence": null,
    "auto_execute": false,
    "blocked_by": [],
    "estimated_edit_scope": "trivial"
  },
  {
    "finding_id": "M-CE-6",
    "severity": "MEDIUM",
    "surface": "core_engine",
    "script_tier": null,
    "dimension": "dead_code",
    "title": "Dead credential-cleanup finally block with `or True` tautology",
    "files": ["scheduled_input_engine/engine.py"],
    "locations": [{"type": "lines", "start": 414, "end": 418}],
    "problem": "`finally: if attempt == max_retries or True: pass` is unconditional dead code. The author intended to clear credentials between retries but left only a comment; real cleanup happens at `del creds` line 420 (outside the loop). Confuses future maintainers.",
    "fix_instruction": "Delete lines 414-418 entirely (the `finally:` block including the comment). The `del creds` at line 420 stays. Verify tests/test_feeder_fixes.py still passes.",
    "done_when": "grep -n 'or True' scheduled_input_engine/engine.py returns zero matches; pytest tests/test_feeder_fixes.py -vv passes.",
    "runtime_validated": false,
    "validation_evidence": null,
    "auto_execute": false,
    "blocked_by": [],
    "estimated_edit_scope": "trivial"
  },
  {
    "finding_id": "M-CE-7",
    "severity": "MEDIUM",
    "surface": "core_engine",
    "script_tier": null,
    "dimension": "security",
    "title": "CREDENTIALS dict persists in extra globals after script execution",
    "files": ["scheduled_input_engine/engine.py"],
    "locations": [{"type": "lines", "start": 326, "end": 341}],
    "problem": "extra['CREDENTIALS'] = creds is set before the ThreadPoolExecutor call and never explicitly cleared. For long-running ingestion the dict is reachable during the script's execution window.",
    "fix_instruction": "Wrap the ThreadPoolExecutor.submit/result in a try/finally and in finally call extra.pop('CREDENTIALS', None). Do this inside _run_task just before the existing `except` chain.",
    "done_when": "Code review confirms extra.pop('CREDENTIALS', None) in the finally of the submit block; existing tests pass.",
    "runtime_validated": false,
    "validation_evidence": null,
    "auto_execute": false,
    "blocked_by": [],
    "estimated_edit_scope": "trivial"
  },
  {
    "finding_id": "M-CE-8",
    "severity": "MEDIUM",
    "surface": "core_engine",
    "script_tier": null,
    "dimension": "correctness",
    "title": "fillna('') applies string default to numeric columns, breaks downstream aggregations",
    "files": ["query_engine/CmdExecutionBackend.py"],
    "locations": [{"type": "lines", "start": 106, "end": 106}, {"type": "lines", "start": 204, "end": 204}],
    "problem": "result_df.fillna('') makes numeric columns object-dtype; downstream stats sum treats empty string as zero or errors. The diagnostic variant at line 204 has the same bug.",
    "fix_instruction": "Replace both fillna('') calls with a dtype-aware fill: for c in result_df.columns: if pd.api.types.is_numeric_dtype(result_df[c]): continue; result_df[c] = result_df[c].fillna(''). Or remove fillna entirely and leave NaN handling to the display/serializer layer.",
    "done_when": "tests/yaml/tier2_functions/ stats with NaN columns pass.",
    "runtime_validated": false,
    "validation_evidence": null,
    "auto_execute": false,
    "blocked_by": [],
    "estimated_edit_scope": "small"
  },
  {
    "finding_id": "M-CE-9",
    "severity": "MEDIUM",
    "surface": "core_engine",
    "script_tier": null,
    "dimension": "contracts",
    "title": "hasattr exposed in sandbox bypasses attribute allowlist for existence checks",
    "files": ["scheduled_input_engine/executor.py"],
    "locations": [{"type": "lines", "start": 179, "end": 179}],
    "problem": "hasattr(obj, name) returns True/False without going through _safe_getattr. Low-impact - the subsequent getattr is still blocked - but leaks existence of dunder attributes a sandboxed script shouldn't be able to enumerate.",
    "fix_instruction": "Replace restricted['hasattr'] = hasattr with a wrapper that honors _BLOCKED_ATTRS: def _safe_hasattr(obj, name): if name in _BLOCKED_ATTRS: return False; return hasattr(obj, name).",
    "done_when": "New unit test asserts _safe_hasattr(pd.DataFrame(), '__subclasses__') returns False.",
    "runtime_validated": false,
    "validation_evidence": null,
    "auto_execute": false,
    "blocked_by": [],
    "estimated_edit_scope": "trivial"
  },
  {
    "finding_id": "M-CE-10",
    "severity": "MEDIUM",
    "surface": "core_engine",
    "script_tier": null,
    "dimension": "security",
    "title": "allowed_api_domains regex has no length/complexity cap (ReDoS)",
    "files": ["scheduled_input_engine/cache.py"],
    "locations": [{"type": "lines", "start": 68, "end": 73}],
    "problem": "Admin-supplied regex patterns are fullmatch-applied to every hostname of every HTTP request. A malicious/buggy pattern like (a+)+$ on a long hostname causes exponential backtracking.",
    "fix_instruction": "At settings-load time, precompile each pattern; reject patterns longer than 256 characters with a loud logger.error and skip them. Compile with re.compile and cache. Add test asserting a 300-char pattern is rejected.",
    "done_when": "New test test_allowed_api_domains_length_cap passes.",
    "runtime_validated": false,
    "validation_evidence": null,
    "auto_execute": false,
    "blocked_by": [],
    "estimated_edit_scope": "small"
  },
  {
    "finding_id": "L-CE-11",
    "severity": "LOW",
    "surface": "core_engine",
    "script_tier": null,
    "dimension": "dead_code",
    "title": "Stale historical comment about jpype in CmdExecutionBackend",
    "files": ["query_engine/CmdExecutionBackend.py"],
    "locations": [{"type": "lines", "start": 153, "end": 161}],
    "problem": "Multi-line comment narrates the history of a removed module. Valuable context can live in a single line pointing at tests/test_no_jpype.py.",
    "fix_instruction": "Replace the 8-line comment block with a one-liner: '# Identity pass; Java normalization was removed with jpype in 2026-04-21. See tests/test_no_jpype.py.'",
    "done_when": "Comment block trimmed.",
    "runtime_validated": false,
    "validation_evidence": null,
    "auto_execute": false,
    "blocked_by": [],
    "estimated_edit_scope": "trivial"
  },
  {
    "finding_id": "L-CE-12",
    "severity": "LOW",
    "surface": "core_engine",
    "script_tier": null,
    "dimension": "dead_code",
    "title": "Parquet filename suffix is doubled: .system4.system4.parquet",
    "files": ["scheduled_input_engine/parquet_writer.py"],
    "locations": [{"type": "lines", "start": 70, "end": 70}],
    "problem": "Generated filenames end with '.system4.system4.parquet' - likely a copy-paste artefact or legacy naming kept for back-compat. Purely cosmetic but noisy in directory listings.",
    "fix_instruction": "If the double suffix is not load-bearing (search codebase for '.system4.system4' in query/glob patterns), reduce to '.system4.parquet'. Otherwise add a comment explaining why.",
    "done_when": "Either suffix reduced, or a comment documents the reason for the doubling.",
    "runtime_validated": false,
    "validation_evidence": null,
    "auto_execute": false,
    "blocked_by": [],
    "estimated_edit_scope": "trivial"
  },
  {
    "finding_id": "L-CE-13",
    "severity": "LOW",
    "surface": "core_engine",
    "script_tier": null,
    "dimension": "contracts",
    "title": "batch_poller job has no toggle-off path when batch mode disabled at runtime",
    "files": ["query_engine/QueryEngine.py"],
    "locations": [{"type": "lines", "start": 568, "end": 574}],
    "problem": "The batch_poller job is added at scheduler startup if settings enable it; disabling the setting later does not remove the job. Wasted CPU polling.",
    "fix_instruction": "Add a settings-change hook that, when claude_analyzer_enable_batch flips to False, calls scheduler.remove_job('batch_poller', jobstore='default') safely.",
    "done_when": "Manual toggle test removes the job without restart.",
    "runtime_validated": false,
    "validation_evidence": null,
    "auto_execute": false,
    "blocked_by": [],
    "estimated_edit_scope": "small"
  },
  {
    "finding_id": "B-AN-1",
    "severity": "BLOCKER",
    "surface": "analyzer",
    "script_tier": null,
    "dimension": "correctness",
    "title": "parse_response_text crashes with AttributeError when JSON is non-dict",
    "files": ["analyzers/claude_analyzer.py"],
    "locations": [{"type": "symbol", "name": "ClaudeAnalyzer.parse_response_text"}],
    "problem": "After json.loads succeeds, line 485 calls set(data.keys()) unconditionally. If Claude returns a top-level JSON array, string, or number, this raises AttributeError. The exception propagates into the batch poller's generic Exception catch, where the specific shape is lost - the operator sees a nondescript 'Error handling result' log and an empty AnalysisResult.",
    "fix_instruction": "In analyzers/claude_analyzer.py, immediately after the data = json.loads(...) branches in parse_response_text (before the expected_keys check around line 484), insert: if not isinstance(data, dict): parsed['status'] = 'error'; parsed['error_message'] = f'Response JSON is not an object (got {type(data).__name__})'; return parsed. Add a test in tests/test_claude_analyzer.py: def test_parse_non_dict_json(): out = ClaudeAnalyzer.parse_response_text('[1,2,3]'); assert out['status'] == 'error'; assert 'not an object' in out['error_message'].",
    "done_when": "pytest tests/test_claude_analyzer.py -k non_dict -vv passes.",
    "runtime_validated": false,
    "validation_evidence": "Inspection-obvious: set(list.keys()) raises AttributeError in the Python data model - no runtime reproduction needed for a one-line defensive guard.",
    "auto_execute": true,
    "blocked_by": [],
    "estimated_edit_scope": "trivial"
  },
  {
    "finding_id": "H-AN-2",
    "severity": "HIGH",
    "surface": "analyzer",
    "script_tier": null,
    "dimension": "observability",
    "title": "_extract_response_text returns empty string when Claude emits no text blocks - no warning",
    "files": ["alert_groups/dispatcher.py"],
    "locations": [{"type": "symbol", "name": "AlertGroupDispatcher._extract_response_text"}],
    "problem": "If response.content holds only tool_use blocks (e.g., an incomplete web_search turn), parts stays empty and the method returns ''. Downstream the dispatcher records status=success, emails a blank body, and the pick-extractor sees no JSON. The operator gets a 'successful' blank brief.",
    "fix_instruction": "After the for block in response.content loop in _extract_response_text, add: if not parts: logger.warning('[!] Claude response contained no text blocks (stop_reason=%s, content_types=%s)', getattr(response, 'stop_reason', None), [type(b).__name__ for b in response.content]). At the dispatch call site, check if the extracted text is empty and route to the failure-email path instead of the success path. Add tests/test_alert_group_robustness.py::test_tool_only_response_marks_failure asserting an AG run with a tool-only response logs the warning and records status='error'.",
    "done_when": "New test passes.",
    "runtime_validated": false,
    "validation_evidence": null,
    "auto_execute": false,
    "blocked_by": [],
    "estimated_edit_scope": "small"
  },
  {
    "finding_id": "H-AN-3",
    "severity": "HIGH",
    "surface": "analyzer",
    "script_tier": null,
    "dimension": "correctness",
    "title": "JSON-in-fence parse does not strip inner whitespace; prose-before-JSON fails",
    "files": ["analyzers/claude_analyzer.py"],
    "locations": [{"type": "symbol", "name": "ClaudeAnalyzer.parse_response_text"}],
    "problem": "Line 471 regex `r\"```(?:json)?\\s*\\n?(.*?)\\n?```\"` plus DOTALL leaves inner content untrimmed and fails when Claude writes 'Here is the JSON:\\n{...}' without fences. The inner json.loads then fails and returns a generic 'Failed to parse response as JSON' with no clue about the actual shape.",
    "fix_instruction": "In parse_response_text: (1) Change json.loads(json_match.group(1)) to json.loads(json_match.group(1).strip()). (2) Before returning the generic error, attempt a brace-balanced extract: find the first '{' and matching '}' in raw_text and try json.loads on that substring. (3) Add fixture tests: test_leading_whitespace_in_fence, test_prose_before_unfenced_json, test_fenced_markdown_non_json_falls_through_cleanly - each asserting distinct error messages or successful parses.",
    "done_when": "All 3 new fixture tests pass.",
    "runtime_validated": false,
    "validation_evidence": null,
    "auto_execute": false,
    "blocked_by": ["B-AN-1"],
    "estimated_edit_scope": "small"
  },
  {
    "finding_id": "H-AN-4",
    "severity": "HIGH",
    "surface": "analyzer",
    "script_tier": null,
    "dimension": "correctness",
    "title": "_budget_fit_results slice assumes JSON is a list; no-ops on dict/scalar",
    "files": ["alert_groups/dispatcher.py"],
    "locations": [{"type": "lines", "start": 998, "end": 1005}],
    "problem": "After json.loads, `data = data[:new_rows]` expects a list. Dict or scalar shapes raise TypeError, swallowed by except Exception: pass, leaving content untrimmed. The loop iterates 10 times computing new_rows but never shrinking content. Budget gate appears to pass but the final prompt still exceeds the cap.",
    "fix_instruction": "After data = json.loads(new_content), branch: if isinstance(data, list): data = data[:new_rows]; elif isinstance(data, dict) and isinstance(data.get('records'), list): data['records'] = data['records'][:new_rows]; elif isinstance(data, dict) and isinstance(data.get('rows'), list): data['rows'] = data['rows'][:new_rows]; else: logger.warning('[!] Cannot trim JSON of shape %s for %s', type(data).__name__, largest.search_name); break (out of the outer trim loop). Then json.dumps(data, default=str). Add tests/test_alert_group_robustness.py::test_budget_fit_dict_shape.",
    "done_when": "Test passes; dispatcher emits the warning for dict shapes.",
    "runtime_validated": false,
    "validation_evidence": null,
    "auto_execute": false,
    "blocked_by": [],
    "estimated_edit_scope": "small"
  },
  {
    "finding_id": "H-AN-5",
    "severity": "HIGH",
    "surface": "analyzer",
    "script_tier": null,
    "dimension": "correctness",
    "title": "estimate_tokens under-counts because it runs on raw content, not the built prompt",
    "files": ["alert_groups/serializer.py", "alert_groups/builder.py", "alert_groups/dispatcher.py"],
    "locations": [{"type": "symbol", "name": "ResultSerializer.estimate_tokens"}, {"type": "symbol", "name": "build_user_content"}, {"type": "symbol", "name": "AlertGroupDispatcher._budget_fit_results"}],
    "problem": "estimate_tokens counts only the per-feeder serialized content; the prompt wrappers from builder.py (section headers, alert-group metadata, timestamps) are not counted. Budget gate approves prompts that exceed the cap once the wrapper overhead is added. Under-estimate can exceed 20% for 10-feeder AGs.",
    "fix_instruction": "Add ResultSerializer.estimate_prompt_tokens(built_content: str) -> int that takes the fully-rendered user_content and returns int(len(built_content)/3.5). In _budget_fit_results, after building the trimmed prompt via builder.build_user_content, call estimate_prompt_tokens and gate on that value. Add tests/test_alert_group_robustness.py::test_budget_estimate_covers_prompt_wrapper that builds a 3-feeder prompt and asserts the estimate covers the markdown wrappers.",
    "done_when": "New test passes; budget gate now rejects prompts that previously slipped through.",
    "runtime_validated": false,
    "validation_evidence": null,
    "auto_execute": false,
    "blocked_by": [],
    "estimated_edit_scope": "small"
  },
  {
    "finding_id": "H-AN-6",
    "severity": "HIGH",
    "surface": "analyzer",
    "script_tier": null,
    "dimension": "correctness",
    "title": "Dual cost ledgers: budget_remaining_cents vs ClaudeHistoryStore.cost_usd",
    "files": ["analyzers/claude_analyzer.py", "analyzers/claude_client.py", "alert_groups/dispatcher.py"],
    "locations": [{"type": "symbol", "name": "ClaudeAnalyzer._record_usage"}, {"type": "symbol", "name": "call_messages_create"}, {"type": "symbol", "name": "AlertGroupDispatcher.dispatch"}],
    "problem": "ClaudeAnalyzer._record_usage stores cost in cents and decrements budget_remaining_cents for scheduled-search analyzer calls. claude_client records cost in USD to ClaudeHistoryStore. Alert-group dispatcher routes directly through claude_client, never touching the analyzer's daily budget counter. A runaway AG schedule can exhaust daily API spend without tripping the budget gate.",
    "fix_instruction": "Consolidate to one ledger. Preferred: move budget counter logic into claude_client so every call_messages_create decrements a shared counter (SQLite row with per-day key). Update ClaudeAnalyzer to read from the same counter. Update dispatcher to depend on the shared gate. Add tests/test_claude_client.py::test_ag_decrements_daily_budget asserting an AG call path decrements the counter observed by ClaudeAnalyzer. See cross-surface finding X-2.",
    "done_when": "New test passes; ClaudeAnalyzer and AG dispatcher read the same counter.",
    "runtime_validated": false,
    "validation_evidence": null,
    "auto_execute": false,
    "blocked_by": [],
    "estimated_edit_scope": "medium"
  },
  {
    "finding_id": "H-AN-7",
    "severity": "HIGH",
    "surface": "analyzer",
    "script_tier": null,
    "dimension": "security",
    "title": "Batch submit payload stored to history without secret redaction",
    "files": ["analyzers/claude_analyzer.py"],
    "locations": [{"type": "symbol", "name": "ClaudeAnalyzer._call_batch_api"}],
    "problem": "_call_batch_api records the full request_payload to ClaudeHistoryStore without calling _redact_kwargs/_scrub_secrets. If the user_content or system prompt ever contains a literal sk-ant-* token (operator paste, injected from data), it lands plaintext in claude_api_history.sqlite.",
    "fix_instruction": "In analyzers/claude_analyzer.py, at the top of _call_batch_api, import from analyzers.claude_client import _redact_kwargs, _scrub_secrets. Before ClaudeHistoryStore.record_call, apply: safe_payload = _redact_kwargs(request_payload); and ensure the nested messages[].content is run through _scrub_secrets. Consider lifting both helpers to analyzers/_scrub.py to avoid cross-module imports. Add tests/test_claude_history.py::test_batch_submit_redacts_sk_ant feeding a payload with fake sk-ant-test and asserting the stored row has [REDACTED].",
    "done_when": "New redaction test passes.",
    "runtime_validated": false,
    "validation_evidence": null,
    "auto_execute": false,
    "blocked_by": [],
    "estimated_edit_scope": "small"
  },
  {
    "finding_id": "M-AN-8",
    "severity": "MEDIUM",
    "surface": "analyzer",
    "script_tier": null,
    "dimension": "security",
    "title": "Email template substitution is one-pass; {{...}} in values survives",
    "files": ["alert_groups/dispatcher.py"],
    "locations": [{"type": "lines", "start": 208, "end": 221}],
    "problem": "Template renderer does `str.replace` once per substitution. If an upstream data field carries {{admin_email}}, it survives the first pass and is rendered literally. Low-severity vector for HTML injection in the branded email.",
    "fix_instruction": "Before substitution, html.escape every value that isn't already HTML. body_html is already rendered HTML and stays trusted; document this with a comment. Reject any substituted value that still contains '{{' after escape; log a warning and strip the delimiters.",
    "done_when": "New test in tests/test_alert_group_robustness.py asserts {{...}}-containing values do not survive to the rendered template.",
    "runtime_validated": false,
    "validation_evidence": null,
    "auto_execute": false,
    "blocked_by": [],
    "estimated_edit_scope": "small"
  },
  {
    "finding_id": "M-AN-9",
    "severity": "MEDIUM",
    "surface": "analyzer",
    "script_tier": null,
    "dimension": "error_handling",
    "title": "Batch poller retrieve() has no retry; transient errors aborts the cycle",
    "files": ["analyzers/batch_poller.py"],
    "locations": [{"type": "symbol", "name": "_process_batch"}],
    "problem": "client.messages.batches.retrieve(batch_id) on a transient HTTP error aborts that batch's poll with a WARNING; next cycle retries from scratch. Also, partial iteration over results has no resume checkpoint - a mid-iteration failure re-handles already-stored results on next entry.",
    "fix_instruction": "Factor out the retry/backoff wrapper from analyzers.claude_client._is_retryable and apply it around retrieve(). Track last_processed_result_index per batch in AnalyzerStorage so re-entry skips handled results. Add tests/test_batch_poller.py::test_retrieve_retries_on_transient_error and ::test_resumes_after_partial_failure.",
    "done_when": "Both tests pass.",
    "runtime_validated": false,
    "validation_evidence": null,
    "auto_execute": false,
    "blocked_by": [],
    "estimated_edit_scope": "medium"
  },
  {
    "finding_id": "M-AN-10",
    "severity": "MEDIUM",
    "surface": "analyzer",
    "script_tier": null,
    "dimension": "correctness",
    "title": "Timestamps mix UTC and server-local across email subject and budget reset",
    "files": ["alert_groups/dispatcher.py", "analyzers/claude_analyzer.py"],
    "locations": [{"type": "lines", "start": 909, "end": 909}, {"type": "lines", "start": 221, "end": 240}],
    "problem": "build_subject_line uses datetime.now(timezone.utc); ClaudeAnalyzer daily budget reset uses date.today() (local). If the server is in a non-UTC zone, the subject shows a different date than the budget ledger.",
    "fix_instruction": "Standardize on UTC. Change date.today() to datetime.utcnow().date() in ClaudeAnalyzer budget code. Grep `date.today\\(\\)\\|datetime.now\\(\\)(?!.*timezone)` across analyzers/ and alert_groups/ and normalize each hit.",
    "done_when": "Grep returns zero naive now()/today() in the two directories.",
    "runtime_validated": false,
    "validation_evidence": null,
    "auto_execute": false,
    "blocked_by": [],
    "estimated_edit_scope": "small"
  },
  {
    "finding_id": "M-AN-11",
    "severity": "MEDIUM",
    "surface": "analyzer",
    "script_tier": null,
    "dimension": "concurrency",
    "title": "No cross-process lock preventing 'Run Now' racing a cron fire for the same AG",
    "files": ["alert_groups/dispatcher.py", "alert_group_store.py"],
    "locations": [{"type": "symbol", "name": "AlertGroupDispatcher.dispatch"}],
    "problem": "APScheduler max_instances=1 guards cron-to-cron only. A manual Run Now via REST can enter the dispatcher while a cron fire is in progress; both mutate circuit-breaker state in AlertGroupStore without an advisory lock.",
    "fix_instruction": "Add an AlertGroupStore method acquire_dispatch_lock(alert_group_id, timeout=5) using a file lock (fcntl on POSIX, msvcrt on Windows) keyed on the AG id. Dispatcher acquires before mutating state; releases in finally. On contention, return early with a 'dispatch already in progress' log.",
    "done_when": "New tests/test_alert_group_robustness.py::test_concurrent_dispatch_serialized passes.",
    "runtime_validated": false,
    "validation_evidence": null,
    "auto_execute": false,
    "blocked_by": [],
    "estimated_edit_scope": "medium"
  },
  {
    "finding_id": "M-AN-12",
    "severity": "MEDIUM",
    "surface": "analyzer",
    "script_tier": null,
    "dimension": "observability",
    "title": "_extract_response_text drops stop_reason context",
    "files": ["alert_groups/dispatcher.py"],
    "locations": [{"type": "symbol", "name": "AlertGroupDispatcher._extract_response_text"}],
    "problem": "The helper returns only the joined text; callers don't see stop_reason, content-block types, or whether max_tokens was hit. Debugging blank-email incidents requires scrolling raw logs.",
    "fix_instruction": "Refactor to return a tuple (text, meta_dict) where meta_dict = {stop_reason, block_types: [...], block_count: int}. Callers currently using _extract_response_text can accept text only by unpacking; failure-email path logs meta_dict.",
    "done_when": "All callers updated; logs include stop_reason.",
    "runtime_validated": false,
    "validation_evidence": null,
    "auto_execute": false,
    "blocked_by": ["H-AN-2"],
    "estimated_edit_scope": "small"
  },
  {
    "finding_id": "M-AN-13",
    "severity": "MEDIUM",
    "surface": "analyzer",
    "script_tier": null,
    "dimension": "observability",
    "title": "_call_batch_api failure path does not log the error to ClaudeHistoryStore",
    "files": ["analyzers/claude_analyzer.py"],
    "locations": [{"type": "symbol", "name": "ClaudeAnalyzer._call_batch_api"}],
    "problem": "If batches.create raises, only the outer analyze_search_result logs it. No row lands in claude_api_history.sqlite for the failed submit - forensics are incomplete.",
    "fix_instruction": "Wrap batches.create in its own try/except inside _call_batch_api. On exception, call ClaudeHistoryStore.get_instance().record_call(source='batch_submit', status='error', error_class=type(e).__name__, error_message=str(e)[:2000], request_body=_redact_kwargs(request_payload), response_body=None) before re-raising.",
    "done_when": "Test simulating a batches.create failure asserts a row appears in the history store with status='error'.",
    "runtime_validated": false,
    "validation_evidence": null,
    "auto_execute": false,
    "blocked_by": [],
    "estimated_edit_scope": "small"
  },
  {
    "finding_id": "L-AN-14",
    "severity": "LOW",
    "surface": "analyzer",
    "script_tier": null,
    "dimension": "security",
    "title": "group_name and search_name embedded in prompt markdown without html.escape",
    "files": ["alert_groups/builder.py"],
    "locations": [{"type": "lines", "start": 40, "end": 55}],
    "problem": "Operator-controlled YAML fields are concatenated into the markdown prompt. Low real risk but defensive escaping is cheap.",
    "fix_instruction": "import html at top of builder.py; wrap group_name and search_name with html.escape(...) at lines 40 and 55.",
    "done_when": "grep -n 'html.escape' alert_groups/builder.py shows escaping in place.",
    "runtime_validated": false,
    "validation_evidence": null,
    "auto_execute": false,
    "blocked_by": [],
    "estimated_edit_scope": "trivial"
  },
  {
    "finding_id": "L-AN-15",
    "severity": "LOW",
    "surface": "analyzer",
    "script_tier": null,
    "dimension": "correctness",
    "title": "Cost calculation does not assert non-negative",
    "files": ["analyzers/claude_client.py"],
    "locations": [{"type": "lines", "start": 354, "end": 358}],
    "problem": "round(cost, 6) can silently credit a mis-configured pricing-table value (negative). Unlikely but worth an assert.",
    "fix_instruction": "After cost = round(cost, 6), add: if cost < 0: logger.error('[x] Negative cost computed: %f for model %s', cost, model); cost = 0.0.",
    "done_when": "Guard present.",
    "runtime_validated": false,
    "validation_evidence": null,
    "auto_execute": false,
    "blocked_by": [],
    "estimated_edit_scope": "trivial"
  },
  {
    "finding_id": "L-AN-16",
    "severity": "LOW",
    "surface": "analyzer",
    "script_tier": null,
    "dimension": "observability",
    "title": "Budget-trim loop silently exits at 10 iterations",
    "files": ["alert_groups/dispatcher.py"],
    "locations": [{"type": "lines", "start": 990, "end": 1017}],
    "problem": "`for _ in range(10)` exits without warning if budget is still exceeded. Operator has no signal the trim was incomplete.",
    "fix_instruction": "After the for loop, if total still exceeds budget, logger.warning('[!] Budget trim loop exhausted; remaining=%d tokens, budget=%d', total, budget).",
    "done_when": "Warning present.",
    "runtime_validated": false,
    "validation_evidence": null,
    "auto_execute": false,
    "blocked_by": [],
    "estimated_edit_scope": "trivial"
  },
  {
    "finding_id": "H-MI-1",
    "severity": "HIGH",
    "surface": "market_intel",
    "script_tier": null,
    "dimension": "correctness",
    "title": "polymarket_high_probability_pro Kelly formula is degenerate - always yields 0",
    "files": ["script_library/scripts/polymarket_high_probability_pro.json"],
    "locations": [{"type": "symbol", "name": "code"}],
    "problem": "With b = q/p and kelly_full = ((b*p) - q)/b, the expression simplifies to 0 for all valid p. Every row gets kelly_fraction_half=0.0 and size_tier='SMALL'; the sort degenerates to leading_price only. Downstream daily_opportunity_brief treats these fields as actionable sizing guidance; they aren't.",
    "fix_instruction": "Surgical fix: rename the misleading fields and drop the tier. In the code string of polymarket_high_probability_pro.json, (a) drop kelly_fraction_full, kelly_fraction_half, suggested_position_size from the output row; (b) rename expected_value_per_dollar to expected_value_if_price_equals_fair (with value 0.0, honestly labeled); (c) keep implied_edge_vs_50 and payout_multiple as they're genuinely informational; (d) update the description to say 'Assumes market price = true probability; does NOT compute an edge-based Kelly'; (e) change the sort to ['leading_price','volume','liquidity'] descending. Structural fix (deferred, product call): add an external estimated_p input and recompute Kelly as (estimated_p - price) / (1 - price). Apply surgical fix now; open structural fix as a spawned task.",
    "done_when": "Script's output columns no longer include kelly_fraction_* fields; tests/test_script_library.py registry entry updated to match; description reflects the new fields.",
    "runtime_validated": false,
    "validation_evidence": null,
    "auto_execute": false,
    "blocked_by": [],
    "estimated_edit_scope": "small"
  },
  {
    "finding_id": "H-MI-2",
    "severity": "HIGH",
    "surface": "market_intel",
    "script_tier": null,
    "dimension": "correctness",
    "title": "Arb scripts flag opportunities without fee/slippage net-of-costs adjustment",
    "files": ["script_library/scripts/kalshi_polymarket_arbitrage.json", "script_library/scripts/kalshi_polymarket_arbitrage_pro.json", "script_library/scripts/polymarket_cross_platform_arbitrage.json"],
    "locations": [{"type": "symbol", "name": "code"}, {"type": "symbol", "name": "code"}, {"type": "symbol", "name": "code"}],
    "problem": "Raw divergence threshold of 3% is below the ~3-5% all-in round-trip cost (Kalshi 0.5-1% per side + Polymarket ~2% taker + slippage). 'Arb' opportunities at 3% raw are break-even or loss-making.",
    "fix_instruction": "In each of the three scripts' code string: (1) add constants FEE_FRACTION_KALSHI=0.01, FEE_FRACTION_POLYMARKET=0.02, SLIPPAGE=0.01, FEE_ROUNDTRIP=FEE_FRACTION_KALSHI+FEE_FRACTION_POLYMARKET+SLIPPAGE; (2) compute net_edge = abs_div - FEE_ROUNDTRIP and add it as column 'net_edge_pct' (as integer percent); (3) change the filter from abs_div*100 < 3.0 to net_edge < 0.01. Update the description to say 'net of typical fees + slippage'. Update tests/test_script_library.py mocks to verify net_edge_pct appears in the output.",
    "done_when": "All three scripts emit net_edge_pct; test registry verifies the column.",
    "runtime_validated": false,
    "validation_evidence": null,
    "auto_execute": false,
    "blocked_by": [],
    "estimated_edit_scope": "small"
  },
  {
    "finding_id": "H-MI-3",
    "severity": "HIGH",
    "surface": "market_intel",
    "script_tier": null,
    "dimension": "correctness",
    "title": "polymarket_arbitrage_scanner has no liquidity floor",
    "files": ["script_library/scripts/polymarket_arbitrage_scanner.json"],
    "locations": [{"type": "symbol", "name": "code"}],
    "problem": "Event arbitrage claims triggered on YES-sum deviation regardless of total liquidity. A thin market with $100 book shows 5% deviation as noise; trading would move prices 10%+.",
    "fix_instruction": "In polymarket_arbitrage_scanner.json code, add MIN_LIQUIDITY_USD = 10000. Before appending a row, compute total_event_liquidity_usd = sum(float(mk.get('liquidity', 0) or 0) for mk in markets) and skip when < MIN_LIQUIDITY_USD. Add total_event_liquidity_usd as a column so Claude sees the constraint. Update tests/test_script_library.py mock to assert low-liquidity events are filtered.",
    "done_when": "Test asserts thin-market events are filtered out.",
    "runtime_validated": false,
    "validation_evidence": null,
    "auto_execute": false,
    "blocked_by": [],
    "estimated_edit_scope": "trivial"
  },
  {
    "finding_id": "H-MI-4",
    "severity": "HIGH",
    "surface": "market_intel",
    "script_tier": null,
    "dimension": "correctness",
    "title": "Probabilities not clamped to [0,1] - corrupt API values propagate",
    "files": ["script_library/scripts/kalshi_contract_scanner.json", "script_library/scripts/polymarket_high_probability.json", "script_library/scripts/polymarket_high_probability_pro.json"],
    "locations": [{"type": "symbol", "name": "code"}, {"type": "symbol", "name": "code"}, {"type": "symbol", "name": "code"}],
    "problem": "Floats parsed from API responses are not range-checked. An occasional 1.5 or -0.1 (API bug) breaks downstream implied_prob, Kelly math, and sort order.",
    "fix_instruction": "In each script's code, after parsing yes_price/no_price/last_price, clamp: yes_price = max(0.0, min(1.0, float(yes_price))). In pro variants use np.clip(yes_price, 0.0, 1.0). Add a regression test in tests/test_script_library.py feeding a corrupted response {yes_price: 1.5} and asserting the output is clamped to 1.0.",
    "done_when": "Three scripts updated; regression test passes.",
    "runtime_validated": false,
    "validation_evidence": null,
    "auto_execute": false,
    "blocked_by": [],
    "estimated_edit_scope": "trivial"
  },
  {
    "finding_id": "H-MI-5",
    "severity": "HIGH",
    "surface": "market_intel",
    "script_tier": null,
    "dimension": "correctness",
    "title": "polymarket_calibration_analysis_pro feeds NaN into scipy curve_fit",
    "files": ["script_library/scripts/polymarket_calibration_analysis_pro.json"],
    "locations": [{"type": "symbol", "name": "code"}],
    "problem": "bin_empirical = np.where(bin_count>0, bin_freq/bin_count, np.nan). Mask = bin_count>=2 doesn't filter NaN-valued bins; curve_fit receives NaN and returns the initial guess silently.",
    "fix_instruction": "Change the mask to: mask = (bin_count >= 2) & ~np.isnan(bin_empirical). Add an early-out: if mask.sum() < 3: fit_status='insufficient_bins'; else try curve_fit and on RuntimeError set fit_status='fit_failed'. Store fit_status in the output row. Update tests/test_script_library.py to assert behavior on synthetic small-sample fixtures.",
    "done_when": "Regression test with small-sample input produces fit_status='insufficient_bins' instead of garbage fit params.",
    "runtime_validated": false,
    "validation_evidence": null,
    "auto_execute": false,
    "blocked_by": [],
    "estimated_edit_scope": "small"
  },
  {
    "finding_id": "H-MI-6",
    "severity": "HIGH",
    "surface": "market_intel",
    "script_tier": null,
    "dimension": "correctness",
    "title": "Temporal decay script silently skips markets with naive-datetime endDate",
    "files": ["script_library/scripts/polymarket_temporal_decay_pro.json"],
    "locations": [{"type": "symbol", "name": "code"}],
    "problem": "datetime.fromisoformat can yield a naive datetime if Polymarket ever returns an unsuffixed timestamp. end_dt - now (now is tz-aware UTC) then raises TypeError, swallowed by the surrounding try - market vanishes with no log.",
    "fix_instruction": "After the try/except parsing end_dt, add: if end_dt.tzinfo is None: logger.warning('[!] Naive endDate for market %s; forcing UTC', m.get('id')); end_dt = end_dt.replace(tzinfo=timezone.utc). Then: if end_dt <= now: continue (and log). Add tests/test_script_library.py fixture with naive-datetime response; assert warning fires and market is processed.",
    "done_when": "Regression test passes.",
    "runtime_validated": false,
    "validation_evidence": null,
    "auto_execute": false,
    "blocked_by": [],
    "estimated_edit_scope": "trivial"
  },
  {
    "finding_id": "M-MI-7",
    "severity": "MEDIUM",
    "surface": "market_intel",
    "script_tier": null,
    "dimension": "correctness",
    "title": "Sentiment word-set scoring ignores negation",
    "files": ["script_library/scripts/polymarket_news_sentiment_divergence.json"],
    "locations": [{"type": "symbol", "name": "code"}],
    "problem": "Overlap with pos/neg word sets treats 'will NOT win' as positive. Aggregates drift based on rhetoric style, not direction.",
    "fix_instruction": "Add a simple negation window: if any word from {'not','no','never','fail'} appears within 3 tokens before a pos/neg hit, flip its polarity. As a cheaper alternative, gate the sentiment_score column with a reliability flag - if sample_size < 3 or |score| < 0.2, mark reliability='UNRELIABLE'. Document in the description that this is a heuristic.",
    "done_when": "Updated script; test fixture with 'will NOT win' scores negative.",
    "runtime_validated": false,
    "validation_evidence": null,
    "auto_execute": false,
    "blocked_by": [],
    "estimated_edit_scope": "small"
  },
  {
    "finding_id": "M-MI-8",
    "severity": "MEDIUM",
    "surface": "market_intel",
    "script_tier": null,
    "dimension": "correctness",
    "title": "polymarket_market_movers_pro compares Gamma snapshot vs CLOB without timestamp check",
    "files": ["script_library/scripts/polymarket_market_movers_pro.json"],
    "locations": [{"type": "symbol", "name": "code"}],
    "problem": "gamma_yes may be minutes-to-hours old; clob_mid is real-time. Large 'delta' can be normal movement, not a signal.",
    "fix_instruction": "Capture gamma_timestamp from lastTradeTime or equivalent in the Gamma response. Skip rows where (now - gamma_timestamp).total_seconds() > 300. Add snapshot_age_seconds as an output column.",
    "done_when": "Updated script emits snapshot_age_seconds and filters stale snapshots.",
    "runtime_validated": false,
    "validation_evidence": null,
    "auto_execute": false,
    "blocked_by": [],
    "estimated_edit_scope": "small"
  },
  {
    "finding_id": "M-MI-9",
    "severity": "MEDIUM",
    "surface": "market_intel",
    "script_tier": null,
    "dimension": "correctness",
    "title": "Kelly sizing tiers are bankroll-agnostic",
    "files": ["script_library/scripts/polymarket_high_probability_pro.json"],
    "locations": [{"type": "symbol", "name": "code"}],
    "problem": "Fixed thresholds at 10%/4% of fractional Kelly are not meaningful without a bankroll reference.",
    "fix_instruction": "Add a bankroll_usd read from global_settings.yaml (default 10000). Output position_size_usd = bankroll_usd * kelly_half. Bucket size_tier from position_size_usd: >=$5000 LARGE, >=$500 MEDIUM, else SMALL. This finding is partially moot if H-MI-1's structural fix lands; mark as blocked_by H-MI-1.",
    "done_when": "Test asserts size_tier varies with the bankroll setting.",
    "runtime_validated": false,
    "validation_evidence": null,
    "auto_execute": false,
    "blocked_by": ["H-MI-1"],
    "estimated_edit_scope": "small"
  },
  {
    "finding_id": "M-MI-10",
    "severity": "MEDIUM",
    "surface": "market_intel",
    "script_tier": null,
    "dimension": "correctness",
    "title": "Calibration script assumes binary markets; multi-outcome events silently skipped",
    "files": ["script_library/scripts/polymarket_calibration_analysis.json"],
    "locations": [{"type": "symbol", "name": "code"}],
    "problem": "Scripts assume len(prices) == 2. Three-way elections or commodity-range events are silently skipped or misclassified.",
    "fix_instruction": "Before processing, if len(prices) > 2: emit a 'skipped_multi_outcome' counter (module-level) and continue. At end of loop, add a single row to the output with market_id='_summary' and skipped_multi_outcome count. Update the description.",
    "done_when": "Summary row appears in fixture tests with multi-outcome events.",
    "runtime_validated": false,
    "validation_evidence": null,
    "auto_execute": false,
    "blocked_by": [],
    "estimated_edit_scope": "small"
  },
  {
    "finding_id": "M-MI-11",
    "severity": "MEDIUM",
    "surface": "market_intel",
    "script_tier": null,
    "dimension": "correctness",
    "title": "Volume/liquidity null-handling silent-falls to zero",
    "files": ["script_library/scripts/kalshi_volume_tracker.json", "script_library/scripts/polymarket_arbitrage_scanner.json"],
    "locations": [{"type": "symbol", "name": "code"}, {"type": "symbol", "name": "code"}],
    "problem": "int(m.get('volume', 0) or 0) pattern merges 'no volume' with 'unknown volume'. Downstream vol_oi_ratio etc. computes on 0 silently.",
    "fix_instruction": "Separate the two cases: raw = m.get('volume'); if raw is None or (isinstance(raw,str) and not raw.replace('.','').isdigit()): volume = None; continue (skip the row for volume-dependent signals). Else volume = int(float(raw)).",
    "done_when": "Updated scripts; test fixture with null volume skips the market.",
    "runtime_validated": false,
    "validation_evidence": null,
    "auto_execute": false,
    "blocked_by": [],
    "estimated_edit_scope": "small"
  },
  {
    "finding_id": "M-MI-12",
    "severity": "MEDIUM",
    "surface": "market_intel",
    "script_tier": null,
    "dimension": "correctness",
    "title": "Entropy calculation numerically unstable near price_sum≈0",
    "files": ["script_library/scripts/polymarket_cross_market_correlation_pro.json"],
    "locations": [{"type": "symbol", "name": "code"}],
    "problem": "if price_sum > 0 admits tiny positive sums where shares = prices_arr/price_sum produces extreme values. scipy.stats.entropy can produce inf.",
    "fix_instruction": "Change the guard to if price_sum > 0.01. After computing shares, apply shares = np.clip(shares, 1e-9, 1.0). Renormalize: shares = shares / shares.sum().",
    "done_when": "Fixture test with price_sum=0.005 produces entropy=None (or a sentinel), not inf.",
    "runtime_validated": false,
    "validation_evidence": null,
    "auto_execute": false,
    "blocked_by": [],
    "estimated_edit_scope": "trivial"
  },
  {
    "finding_id": "L-MI-13",
    "severity": "LOW",
    "surface": "market_intel",
    "script_tier": null,
    "dimension": "architecture",
    "title": "Market-data scripts do not use requests.Session for multi-page pulls",
    "files": ["script_library/scripts/polymarket_arbitrage_scanner.json", "script_library/scripts/kalshi_active_markets.json"],
    "locations": [{"type": "symbol", "name": "code"}, {"type": "symbol", "name": "code"}],
    "problem": "Every requests.get opens a new TCP connection. For scripts iterating 5+ pages of 100 markets each, this hits rate limits faster and increases latency.",
    "fix_instruction": "Hoist session = requests.Session() at the top of each multi-request script; call session.get(...) in the loops. Only touches scripts with known pagination (>= 3 pages).",
    "done_when": "Representative scripts updated.",
    "runtime_validated": false,
    "validation_evidence": null,
    "auto_execute": false,
    "blocked_by": [],
    "estimated_edit_scope": "small"
  },
  {
    "finding_id": "L-MI-14",
    "severity": "LOW",
    "surface": "market_intel",
    "script_tier": null,
    "dimension": "contracts",
    "title": "Cross-platform arb action label is correct-by-accident",
    "files": ["script_library/scripts/kalshi_polymarket_arbitrage.json", "script_library/scripts/kalshi_polymarket_arbitrage_pro.json"],
    "locations": [{"type": "symbol", "name": "code"}, {"type": "symbol", "name": "code"}],
    "problem": "Label 'BUY_POLYMARKET_SELL_KALSHI' is semantically fine but the word 'SELL' doesn't reflect the underlying operation on a prediction market (you buy the opposite side). A maintainer editing the subtraction order would silently invert the signal.",
    "fix_instruction": "Replace suggested_action with two explicit columns: polymarket_action (BUY_YES or BUY_NO), kalshi_action (BUY_YES or BUY_NO). Drop the ambiguous composite.",
    "done_when": "Updated scripts emit per-leg actions; registry test updated.",
    "runtime_validated": false,
    "validation_evidence": null,
    "auto_execute": false,
    "blocked_by": [],
    "estimated_edit_scope": "trivial"
  },
  {
    "finding_id": "L-MI-15",
    "severity": "LOW",
    "surface": "market_intel",
    "script_tier": null,
    "dimension": "correctness",
    "title": "Empty-DataFrame schema preservation lacks explicit numeric dtypes",
    "files": ["script_library/scripts/kalshi_polymarket_arbitrage.json", "script_library/scripts/kalshi_polymarket_arbitrage_pro.json"],
    "locations": [{"type": "symbol", "name": "code"}, {"type": "symbol", "name": "code"}],
    "problem": "pd.DataFrame(rows, columns=EXPECTED_COLUMNS) with rows=[] produces object-dtype columns. Downstream SPQL `| where divergence_pct > 3` on object dtype fails.",
    "fix_instruction": "After df = pd.DataFrame(rows, columns=EXPECTED_COLUMNS), if df.empty: df = df.astype({'kalshi_yes_price':'float64','polymarket_yes_price':'float64','divergence':'float64','abs_divergence':'float64','divergence_pct':'float64','_epoch':'int64'}).",
    "done_when": "Zero-row fixture produces a Parquet whose schema matches the non-empty run.",
    "runtime_validated": false,
    "validation_evidence": null,
    "auto_execute": false,
    "blocked_by": [],
    "estimated_edit_scope": "trivial"
  },
  {
    "finding_id": "H-SV-1",
    "severity": "HIGH",
    "surface": "script_exec_vault",
    "script_tier": "both",
    "dimension": "observability",
    "title": "Trust-tier fallback to 'sandboxed' has no audit log",
    "files": ["scheduled_input_engine/engine.py"],
    "locations": [{"type": "lines", "start": 310, "end": 313}, {"type": "lines", "start": 368, "end": 368}, {"type": "lines", "start": 384, "end": 384}, {"type": "lines", "start": 407, "end": 407}],
    "problem": "`trust_level=task.get('trust_level') or 'sandboxed'` silently assumes sandboxed when the field is missing or falsy. A migration bug or manual DB edit could run a pro script as sandboxed (fail with obscure import errors) or leave a pro script mis-marked. Author intent is lost without a signal.",
    "fix_instruction": "Above each of the 4 tag sites in _run_task, add one-shot logging: `if not task.get('trust_level'): logger.warning('[!] Task %s has no explicit trust_level; defaulting to sandboxed', task_id)`. Add a one-shot migration that backfills NULL trust_level → 'sandboxed' in scheduled_inputs.db and adds NOT NULL DEFAULT 'sandboxed' constraint. Add tests/test_store_migrations.py::test_trust_level_not_null.",
    "done_when": "Migration test passes; warning log fires for any legacy row without explicit trust_level.",
    "runtime_validated": false,
    "validation_evidence": null,
    "auto_execute": false,
    "blocked_by": [],
    "estimated_edit_scope": "small"
  },
  {
    "finding_id": "H-SV-2",
    "severity": "HIGH",
    "surface": "script_exec_vault",
    "script_tier": "both",
    "dimension": "security",
    "title": "Credential values may leak to error-message log via str(exc)[:500]",
    "files": ["scheduled_input_engine/engine.py"],
    "locations": [{"type": "lines", "start": 377, "end": 385}, {"type": "lines", "start": 400, "end": 408}],
    "problem": "If a script crashes with an exception whose message embeds credential values (e.g., KeyError({'GITHUB_TOKEN': 'ghp_...'})), the first 500 characters include literal secret material before truncation. That string lands in Parquet telemetry (indexes/logs/ingestion/) and SQLite history.",
    "fix_instruction": "Add a helper `_redact_credentials(msg: str, cred_dict: dict | None) -> str` in scheduled_input_engine/engine.py (or a shared scheduled_input_engine/_redact.py). It replaces every credential value >=4 chars with '[REDACTED:KEY]'. Apply to error_message before the record_execution and _emit_ingestion_log calls at lines 377-408. Add tests/test_feeder_fixes.py::test_credential_redacted_in_error_message raising a ValueError(str({'SECRET': 'ghp_real_long_value'})) from a mock script and asserting the logged message contains '[REDACTED:SECRET]'.",
    "done_when": "New redaction test passes.",
    "runtime_validated": false,
    "validation_evidence": null,
    "auto_execute": false,
    "blocked_by": [],
    "estimated_edit_scope": "small"
  },
  {
    "finding_id": "H-SV-3",
    "severity": "HIGH",
    "surface": "script_exec_vault",
    "script_tier": "pro",
    "dimension": "security",
    "title": "Repo-script subprocess stderr logged verbatim - env-var creds leak if script echoes env",
    "files": ["scheduled_input_engine/engine.py"],
    "locations": [{"type": "lines", "start": 454, "end": 470}],
    "problem": "Credentials are injected as env vars (SPEAKESQUERY_CRED_*) for repo scripts. Subprocess stderr is passed verbatim to self.store.record_execution(error_msg=result.stderr). If the script prints its environment (intentionally or via a misconfigured library), creds reach telemetry.",
    "fix_instruction": "Add a stderr scrubber before record_execution. Regex-scrub any string matching 'SPEAKESQUERY_CRED_[A-Z0-9_]+=[^\\s]+' and any literal credential value from creds.values() that is >=4 chars. Share the helper with H-SV-2's _redact_credentials. Add tests/test_feeder_fixes.py::test_repo_script_stderr_redacts_env that runs a repo-script stub printing `echo $SPEAKESQUERY_CRED_X >&2` and asserts the telemetry row contains [REDACTED]. (A structural fix - move cred transport from env vars to stdin-JSON - is a separate design call, tracked in Deferred.)",
    "done_when": "New stderr-redact test passes.",
    "runtime_validated": false,
    "validation_evidence": null,
    "auto_execute": false,
    "blocked_by": ["H-SV-2"],
    "estimated_edit_scope": "small"
  },
  {
    "finding_id": "M-SV-4",
    "severity": "MEDIUM",
    "surface": "script_exec_vault",
    "script_tier": "both",
    "dimension": "contracts",
    "title": "Empty credentials: {} vs None ambiguity at vault-engine boundary",
    "files": ["scheduled_input_engine/engine.py", "scheduled_input_engine/credentials.py"],
    "locations": [{"type": "lines", "start": 330, "end": 331}, {"type": "symbol", "name": "CredentialsVault.decrypt_for_script"}],
    "problem": "decrypt_for_script returns an empty MappingProxyType({}) when no creds are stored; engine's `if creds is not None:` always enters, injecting CREDENTIALS={} into the sandbox. Scripts accessing CREDENTIALS['API_KEY'] raise KeyError with the specific key name - which is a semantic difference from 'vault failed to load'.",
    "fix_instruction": "In CredentialsVault.decrypt_for_script, return None when no credentials are stored. In _run_task, `if creds:` (falsy-None and falsy-empty both route the same); only inject CREDENTIALS when non-empty. Update tests/test_feeder_fixes.py::test_empty_creds_not_injected.",
    "done_when": "New test passes; existing tests still pass.",
    "runtime_validated": false,
    "validation_evidence": null,
    "auto_execute": false,
    "blocked_by": [],
    "estimated_edit_scope": "small"
  },
  {
    "finding_id": "M-SV-5",
    "severity": "MEDIUM",
    "surface": "script_exec_vault",
    "script_tier": "n/a",
    "dimension": "architecture",
    "title": "Fernet master-key rotation undocumented; in-memory key is stale after rotation",
    "files": ["scheduled_input_engine/credentials.py", "docs/lang/13_backup_recovery.md"],
    "locations": [{"type": "lines", "start": 70, "end": 83}, {"type": "symbol", "name": "Key rotation section"}],
    "problem": "The master key is read once and cached in self._fernet. If a user rotates the file on disk, the running process keeps using the old key; previously-encrypted credentials still decrypt, but newly-encrypted ones mismatch what a restarted process would see. No documented rotation procedure.",
    "fix_instruction": "Add a 'Credential Key Rotation' section to docs/lang/13_backup_recovery.md with the restart-required caveat and a stub command `python -m tools.rotate_vault_key --old-key OLD --new-key NEW`. Add tools/rotate_vault_key.py that re-encrypts every credential row with a new key. Don't automate; document the manual process.",
    "done_when": "Doc section exists; tools/rotate_vault_key.py is a working script even if the UI flow lands later.",
    "runtime_validated": false,
    "validation_evidence": null,
    "auto_execute": false,
    "blocked_by": [],
    "estimated_edit_scope": "medium"
  },
  {
    "finding_id": "M-SV-6",
    "severity": "MEDIUM",
    "surface": "script_exec_vault",
    "script_tier": "restricted",
    "dimension": "testability",
    "title": "Sandbox has no negative regression tests for escape attempts",
    "files": ["tests/test_feeder_fixes.py"],
    "locations": [{"type": "symbol", "name": "TestSandboxEscapeAttempts"}],
    "problem": "No test verifies that a sandboxed script cannot `import os`, `type([]).__bases__[0].__subclasses__()`, `open('/etc/passwd')`. A future edit to _build_sandbox_globals could regress the boundary silently.",
    "fix_instruction": "Add TestSandboxEscapeAttempts with 4 fixtures: (1) bare `import os`; (2) `__import__('os')`; (3) `type([]).__bases__[0].__subclasses__()`; (4) `open('/etc/passwd')`. Each asserts CodeExecutor(code, trust_level='sandboxed').execute_test() returns status='fail' with a meaningful error. For cases that would succeed because they're not enforced (e.g., the __subclasses__ path), either add enforcement or mark those tests xfail with a comment.",
    "done_when": "4 new tests exist; any that xfail link to a follow-up issue.",
    "runtime_validated": false,
    "validation_evidence": null,
    "auto_execute": false,
    "blocked_by": [],
    "estimated_edit_scope": "small"
  },
  {
    "finding_id": "M-SV-7",
    "severity": "MEDIUM",
    "surface": "script_exec_vault",
    "script_tier": "restricted",
    "dimension": "testability",
    "title": "ALLOWED_MODULES drift risk - runtime list and docs table can diverge",
    "files": ["scheduled_input_engine/executor.py", "docs/lang/09_ingestion_etiquette.md"],
    "locations": [{"type": "symbol", "name": "ALLOWED_MODULES"}, {"type": "symbol", "name": "Allowed modules table"}],
    "problem": "The enforced allowlist lives in executor.py; docs/lang/09_ingestion_etiquette.md lists the same modules in prose. Drift is possible and has no test.",
    "fix_instruction": "Add tests/test_sandbox_module_doc_drift.py that parses the 'Allowed modules' markdown table from docs/lang/09_ingestion_etiquette.md and asserts the set matches sorted(ALLOWED_MODULES.keys()).",
    "done_when": "Drift test passes; any future change touches both files together.",
    "runtime_validated": false,
    "validation_evidence": null,
    "auto_execute": false,
    "blocked_by": [],
    "estimated_edit_scope": "small"
  },
  {
    "finding_id": "M-SV-8",
    "severity": "MEDIUM",
    "surface": "script_exec_vault",
    "script_tier": "pro",
    "dimension": "correctness",
    "title": "Repo-script env-var injection upper-cases keys - case-only credential collisions stomp",
    "files": ["scheduled_input_engine/engine.py"],
    "locations": [{"type": "lines", "start": 457, "end": 457}],
    "problem": "env[f'SPEAKESQUERY_CRED_{k.upper()}'] = v. Two creds named 'api_key' and 'API_KEY' both map to SPEAKESQUERY_CRED_API_KEY. Second write wins silently.",
    "fix_instruction": "Before the loop, detect collisions: upper_keys = [k.upper() for k in creds]; if len(set(upper_keys)) != len(upper_keys): raise ValueError(f'Credential name collision after case-normalization for task {script_id}: {upper_keys}'). Caller logs and skips the script.",
    "done_when": "Added guard; tests/test_feeder_fixes.py::test_repo_script_credential_case_collision passes.",
    "runtime_validated": false,
    "validation_evidence": null,
    "auto_execute": false,
    "blocked_by": [],
    "estimated_edit_scope": "trivial"
  },
  {
    "finding_id": "L-SV-9",
    "severity": "LOW",
    "surface": "script_exec_vault",
    "script_tier": "both",
    "dimension": "security",
    "title": "cache.is_allowed_api_url shares the ReDoS exposure with M-CE-10",
    "files": ["scheduled_input_engine/cache.py"],
    "locations": [{"type": "lines", "start": 43, "end": 75}],
    "problem": "Same regex complexity gap as M-CE-10; restated here because it gates every HTTP request made from both tiers.",
    "fix_instruction": "Share the precompile+length-cap fix from M-CE-10; no separate change needed.",
    "done_when": "M-CE-10 done_when satisfied.",
    "runtime_validated": false,
    "validation_evidence": null,
    "auto_execute": false,
    "blocked_by": ["M-CE-10"],
    "estimated_edit_scope": "trivial"
  },
  {
    "finding_id": "L-SV-10",
    "severity": "LOW",
    "surface": "script_exec_vault",
    "script_tier": "n/a",
    "dimension": "architecture",
    "title": "Credentials table has no version column for rotation history",
    "files": ["scheduled_input_engine/credentials.py"],
    "locations": [{"type": "lines", "start": 28, "end": 37}],
    "problem": "Credential rotation loses prior versions silently. Acceptable for a local-trust desktop app; worth recording.",
    "fix_instruction": "If/when rotation is formalized, add version INTEGER DEFAULT 1 and bump on every update. Keep a configurable retention count.",
    "done_when": "Deferred until rotation feature lands.",
    "runtime_validated": false,
    "validation_evidence": null,
    "auto_execute": false,
    "blocked_by": ["M-SV-5"],
    "estimated_edit_scope": "small"
  },
  {
    "finding_id": "X-1",
    "severity": "HIGH",
    "surface": "cross_surface",
    "script_tier": null,
    "dimension": "error_handling",
    "title": "Silent-failure pattern: `except Exception: pass` recurs at 4+ hot paths",
    "files": ["query_engine/CmdExecutionBackend.py", "alert_groups/dispatcher.py", "analyzers/batch_poller.py", "scheduled_input_engine/engine.py"],
    "locations": [{"type": "lines", "start": 115, "end": 117}, {"type": "lines", "start": 1004, "end": 1005}, {"type": "lines", "start": 103, "end": 108}, {"type": "lines", "start": 458, "end": 459}],
    "problem": "The same anti-pattern - catch-all Exception + swallow + no (or generic) log - recurs at core engine, analyzer, batch poller, and credential injection. User-visible symptom is always 'didn't fire' / 'blank result' with nothing useful in logs.",
    "fix_instruction": "Create functionality/error_logging.py exposing log_and_swallow(context: str, exc: BaseException, *, level='error') that emits structured `[x] <context>: <ExceptionClass>: <message>`. Replace each bare-pass site with log_and_swallow('descriptive_context', exc). Exclude documented-allowlist sites (mark with a single-line comment). Add tests/test_silent_failure_coverage.py that greps the repo (excluding tests/) for `except Exception:\\s*pass` and asserts count==0 outside the allowlist.",
    "done_when": "Coverage test passes; `rg 'except Exception:\\s*pass' --type py` returns only allowlisted hits.",
    "runtime_validated": false,
    "validation_evidence": null,
    "auto_execute": false,
    "blocked_by": [],
    "estimated_edit_scope": "medium"
  },
  {
    "finding_id": "X-2",
    "severity": "HIGH",
    "surface": "cross_surface",
    "script_tier": null,
    "dimension": "correctness",
    "title": "Dual cost ledgers: budget_remaining_cents vs ClaudeHistoryStore.cost_usd",
    "files": ["analyzers/claude_analyzer.py", "analyzers/claude_client.py", "alert_groups/dispatcher.py"],
    "locations": [{"type": "symbol", "name": "ClaudeAnalyzer._record_usage"}, {"type": "symbol", "name": "call_messages_create"}, {"type": "symbol", "name": "AlertGroupDispatcher.dispatch"}],
    "problem": "Two independent cost paths: ClaudeAnalyzer tracks cents and decrements a per-day budget counter used only for scheduled-search analyzer calls. claude_client tracks USD in ClaudeHistoryStore. Alert-group dispatcher calls claude_client directly and never touches the analyzer budget. Runaway AG schedules can exceed the intended daily spend without tripping the gate.",
    "fix_instruction": "Consolidate into one ledger. Preferred: move budget counter state into a new table budget_ledger in claude_api_history.sqlite (or a new analyzer_budget.sqlite) with per-day cost_usd totals. claude_client.call_messages_create increments; ClaudeAnalyzer and AG dispatcher both read the same counter. Migration: re-compute today's spend from ClaudeHistoryStore at first boot. Add end-to-end test: scheduled analyzer call + AG call both increment the counter; a gate set to 0.01 blocks the next call.",
    "done_when": "E2E test passes; cent-based counter retired or documented as a derived view.",
    "runtime_validated": false,
    "validation_evidence": null,
    "auto_execute": false,
    "blocked_by": [],
    "estimated_edit_scope": "medium"
  }
]
