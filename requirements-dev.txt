antlr4-python3-runtime~=4.13.1
pandas~=2.2.3
pyyaml~=6.0.2
defusedxml~=0.7
pytest~=8.1
flake8~=7.0
bandit~=1.7
flask-limiter~=3.5.0

# ── UI testing (Playwright) ───────────────────────────────────────────────────
# Browser automation for automated UI regression tests.
# After install, run:  playwright install chromium
pytest-playwright~=0.5

# ── Pre-commit secret detection ──────────────────────────────────────────────
# Prevents accidental credential commits. Activated automatically by setup.sh.
pre-commit>=4.0
detect-secrets>=1.5
