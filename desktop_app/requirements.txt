# ─────────────────────────────────────────────────────────────────────────────
# SpeakesQuery Desktop - Local development dependencies
#
# Setup (recommended - use setup.sh):
#   1. Run bootstrap:     ./setup.sh            # creates env/, installs deps, verifies packages
#   2. Activate it:       source env/bin/activate
#   3. Run server:        python desktop_app/server.py
#   4. Open browser:      http://localhost:5111
#
# Manual setup (alternative):
#   1. Create a venv:     python3.12 -m venv env
#   2. Activate it:       source env/bin/activate
#   3. Install deps:      pip install -r requirements.txt
#   4. Run server:        python desktop_app/server.py
#
# PyCharm CE:
#   • Set project interpreter → env/
#   • Run/Debug Configuration → Script: desktop_app/server.py
#   • Working directory: project root
#   • Set breakpoints, click Debug - it just works
# ─────────────────────────────────────────────────────────────────────────────

# ── Core SpeakesQuery engine deps (mirrors project root requirements.txt) ──────
pandas~=2.2.3
pyarrow~=17.0.0
requests~=2.32.3
antlr4-python3-runtime~=4.13.1
defusedxml~=0.7
flask~=3.1.0
werkzeug~=3.1.3
croniter~=6.0.0
numpy~=2.2.1
flask-limiter~=3.5.0
flask-login~=0.6.3
pyyaml~=6.0.2
pyparsing~=3.2.0
aiosmtplib~=3.0.2
aiosqlite~=0.20.0
apscheduler>=3,<4
restrictedpython~=7.4
flask-wtf~=1.2.1
python-dotenv~=1.1
faker~=37.4.0
cryptography~=42.0
beautifulsoup4~=4.12
lxml~=5.2
certifi>=2024.0

# ── C++ extension build deps ─────────────────────────────────────────────────
pybind11
