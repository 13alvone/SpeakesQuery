"""Operational tools shipped with SpeakesQuery (diagnostics, one-off repair utilities).

These modules are included in the Docker image (unlike `tests/`) so they
can be run from inside a deployed container via `python -m tools.<name>`.
"""

